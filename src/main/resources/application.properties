# Application Configuration
spring.application.name=workzen-hrms
server.port=8081
spring.main.allow-circular-references=true

# # Database Configuration - H2 (Development)
# spring.datasource.url=jdbc:h2:mem:workzen_hrms
# spring.datasource.username=Aryan
# spring.datasource.password=8252
# spring.datasource.driver-class-name=org.h2.Driver
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

spring.datasource.url=jdbc:postgresql://localhost:5432/workzen_hrms?sslmode=disable
spring.datasource.username=postgres
spring.datasource.password=${DB_PASSWORD:8252}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.defer-datasource-initialization=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# JWT Configuration
# HS512 requires at least 512 bits (64 characters) for security
jwt.secret=${JWT_SECRET:LWaU2xft+klUHMvPMvZhEuSjVNhPQtYkfyGcbyK6Ti5IYXnTAclxaBYLkODhEmTtSecureKeyForHS512AlgorithmWithMinimum64Characters}
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# Email Configuration
# Set to false to disable email sending during development
spring.mail.enabled=true

# Gmail Configuration
# Enable "App Passwords" in your Google Account settings
# Go to: https://myaccount.google.com/apppasswords
# Generate an app password and use it here
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username= _________________
spring.mail.password= _________________
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Sender email address
spring.mail.from=__________________

# Application Configuration for Emails
app.company.name=WorkZen HRMS
app.frontend.url=http://localhost:8080
app.password-reset.expiration=86400000

# File Upload Configuration
file.upload-dir=uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.enabled=true

# Logging Configuration
logging.level.com.workzen=DEBUG
# logging.level.org.springframework = DEBUG
logging.level.org.springframework.security=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
